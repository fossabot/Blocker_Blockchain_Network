name: Deploy Blockchain Server

on:
  push:
    paths:
      - 'blockchain-server/**'
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push blockchain-server image
        uses: docker/build-push-action@v5
        with:
          context: ./blockchain-server
          file: ./blockchain-server/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/blockchain-server:latest

      - name: Prepare SSH key
        run: |
          echo "${{ secrets.EC2_KEY }}" | base64 -d > ec2_key.pem
          chmod 600 ec2_key.pem

      - name: Restart blockchain-server, registry-service, update_service on EC2
        run: |
          ssh -i ec2_key.pem -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} "\
            cd ~/blockchain-network/blockchain-server && \
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/blockchain-server:latest && \
            docker-compose down || true && \
            docker-compose up -d && \
            cd ~/blockchain-network/registry-service && \
            docker-compose down || true && \
            docker-compose up -d && \
            cd ~/blockchain-network/update_service && \
            docker-compose down || true && \
            docker-compose up -d
          "
